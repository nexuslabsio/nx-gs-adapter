package app.l2nx.gs.adapter.api.kafka.events.olympiad;

/**
 * Olympiad 1v1 game type. Teams (3v3) ships as a separate subtype when
 * needed.
 */
public enum OlympiadGameType {
    CLASSED,
    NONCLASSED
}
